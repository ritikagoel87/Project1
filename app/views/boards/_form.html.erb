<%= form_for @board do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, :autofocus => true, :required => true %>
  <%= f.label :image %>
  <%= f.text_field :image %>
  <%= f.hidden_field :user_id, :value => @current_user.id %>

  <%= f.submit "Create" %>
<% end %>
